version: '3'
services:
  db:
    image: postgres
    container_name: localdb
    restart: always
    volumes:
      - ./dump/:/data/db
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: super_coole_blog_db
      POSTGRES_DB: blog
