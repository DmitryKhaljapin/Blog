generator client {
  provider = "prisma-client-js"
  binaryTargets = ["native", "linux-musl-openssl-3.0.x"]
}

datasource db {
  provider = "postgres"
  url      = env("DB_URI")
}

model UserModel {
  id             Int          @id @default(autoincrement())
  name String
  email          String       @unique
  password       String
  isActivated    Boolean      @default(false)
  activationLink String
  TokenModel     TokenModel[]
  PostModel PostModel[]
  CommentModel CommentModel[]
}

model TokenModel {
  id           Int        @id @default(autoincrement())
  refreshToken String     @unique
  userId       Int        @unique
  user         UserModel  @relation(fields: [userId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@index([userId])
}

model PostModel {
  id Int @id @default(autoincrement())
  title String
  content String
  createdAt DateTime
  userId Int
  user UserModel  @relation(fields: [userId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@index([userId])
  CommentModel CommentModel[]
}

model CommentModel {
  id Int @id @default(autoincrement())
  text String
  createdAt DateTime
  userId Int
  postId Int
  user UserModel  @relation(fields: [userId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@index([userId])
  post PostModel  @relation(fields: [postId], references: [id], onDelete: NoAction, onUpdate: NoAction)
  @@index([postId])
}